# ============================================================================
# Cloud Fuel & Dispensing Platform — Python requirements
# ============================================================================

# Web framework & server
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Settings / validation
pydantic>=2.7,<3.0
pydantic-settings>=2.3

# Database (async SQLAlchemy 2.0 + TimescaleDB/PostgreSQL)
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29
psycopg[binary]>=3.1            # sync driver for Alembic migrations
alembic>=1.13

# Cache / broker
redis>=5.0

# Security
PyJWT>=2.8
passlib[bcrypt]>=1.7
bcrypt>=4.1

# MQTT (EMQX broker)
paho-mqtt>=2.0

# Excel / CSV ingestion
openpyxl>=3.1

# Async workers
celery>=5.3

# ============================================================================
# Development / tests
# ============================================================================
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27